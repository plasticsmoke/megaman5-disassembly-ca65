; =============================================================================
; Mega Man 5 (U) — Zero Page Variable Definitions
; Only confirmed variables from NMI/game loop/entity code tracing
; =============================================================================
; SKELETON — populated as code tracing confirms each variable.
; MM5 shares the MM4 engine lineage; do NOT assume MM4 addresses carry
; over — every entry here must be verified against MM5 code.
